apiVersion: apps/v1
kind: Deployment
metadata:
  name: live-read-service
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: live-read-service
  template:
    metadata:
      labels:
        app: live-read-service
    spec:
      containers:
      - name: live-read-service
        image: {{ .Values.registry }}/live-read-service:{{ .Values.liveReadImageTag }}
        imagePullPolicy: Always
        ports:
        - containerPort: 8191
        env:
        - name: JAVA_OPTS
          value: "-Xms128m -Xmx256m -XX:+UseSerialGC -XX:MaxMetaspaceSize=96m"
        - name: SPRING_PROFILES_ACTIVE
          value: "k8s"
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_BUSINESS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_BUSINESS_PORT
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: REDIS_BUSINESS_PASSWORD
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: KAFKA_BUSINESS_BOOTSTRAP_SERVERS
        - name: INTERNAL_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: INTERNAL_API_KEY
        - name: LIVE_WRITE_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: LIVE_WRITE_SERVICE_URL
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8191
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "320Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: live-read-service
  namespace: dev
spec:
  selector:
    app: live-read-service
  ports:
  - port: 8191
    targetPort: 8191
