apiVersion: apps/v1
kind: Deployment
metadata:
  name: live-query-service
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: live-query-service
  template:
    metadata:
      labels:
        app: live-query-service
    spec:
      containers:
      - name: live-query-service
        image: {{ .Values.registry }}/live-query-service:{{ .Values.liveQueryImageTag }}
        imagePullPolicy: Always
        ports:
        - containerPort: 8101
        env:
        - name: JAVA_OPTS
          value: "-Xms128m -Xmx256m -XX:+UseSerialGC -XX:MaxMetaspaceSize=96m"
        - name: SPRING_PROFILES_ACTIVE
          value: "k8s"
        - name: INTERNAL_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: INTERNAL_API_KEY
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_BUSINESS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REDIS_BUSINESS_PORT
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: REDIS_BUSINESS_PASSWORD
        - name: BOOKING_DB_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: BOOKING_DB_HOST
        - name: BOOKING_DB_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: BOOKING_DB_PORT
        - name: BOOKING_DB_USER
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: BOOKING_DB_USER
        - name: BOOKING_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: BOOKING_DB_PASSWORD
        - name: LIVE_READ_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: LIVE_READ_SERVICE_URL
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8101
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "320Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: live-query-service
  namespace: dev
spec:
  selector:
    app: live-query-service
  ports:
  - port: 8101
    targetPort: 8101
