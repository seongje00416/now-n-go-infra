apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-products
  namespace: dev
spec:
  serviceName: postgres-products
  replicas: 1
  selector:
    matchLabels:
      app: postgres-products
  template:
    metadata:
      labels:
        app: postgres-products
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: "products_db"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: PRODUCT_DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: PRODUCT_DB_PASSWORD
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "productservice", "-d", "products_db"]
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "48Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "150m"
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-products
  namespace: dev
spec:
  selector:
    app: postgres-products
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None
