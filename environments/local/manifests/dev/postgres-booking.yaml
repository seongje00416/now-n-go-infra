apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-booking
  namespace: dev
spec:
  serviceName: postgres-booking
  replicas: 1
  selector:
    matchLabels:
      app: postgres-booking
  template:
    metadata:
      labels:
        app: postgres-booking
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: "booking_db"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: BOOKING_DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: BOOKING_DB_PASSWORD
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "bookingservice", "-d", "booking_db"]
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-booking
  namespace: dev
spec:
  selector:
    app: postgres-booking
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None
